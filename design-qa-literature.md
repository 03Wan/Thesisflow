# ThesisFlow 文献研究视觉检查

- 参考：`D:/CDriveRelief/Temp/codex-clipboard-07d5c716-3d00-4967-8ff0-857b2677e1ac.png`
- 实现：`/literature`
- 验收视口：1430 × 906，并补测默认 1280 宽度

## 检查结果

- 信息架构：通过。保持全局导航，并恢复文献分组、中央检索表格、AI 文献助手三栏结构。
- 主体比例：通过。内侧导航约 170px，AI 助手约 280px，中央区域自适应。
- 视觉语言：通过。沿用 ThesisFlow 的蓝色强调、浅灰背景、白色细边框卡片和紧凑桌面字号。
- 数据边界：通过。统计、智能视图、矩阵、标签与年份分布均来自真实 `WorkspaceRow`；空库不注入示例记录。
- 桌面响应式：通过。1430px 保留三个底部分析卡；1280px 收敛底部内容但保留三栏主工作区。
- 关键状态：通过。空库、读取失败、搜索无结果、批量操作和无全文禁用状态均有明确反馈。
- 交互抽检：通过。新建文献弹窗可打开/关闭；搜索会切换为无匹配状态；智能视图可切换。
- Source viewer 边界：通过。只有存在 `primaryFileId` 时才允许调用 `fileService.openLocation()`。

## 与参考图的有意差异

- 参考图中的 18 条演示数据未复用；无真实文献时显示空库状态。
- 数据源筛选替换为与当前领域模型一致的智能视图；在线发现保留独立入口。
- AI 摘要改为 Evidence-first 状态说明，不展示缺少来源证据的模拟结论。

final result: passed
