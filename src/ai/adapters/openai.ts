import type { AIRequest, AIResponse, AIStreamEvent, ResolvedProviderConfig } from "@/ai/domain";
import { HttpProviderAdapter, parseStructured, promptText, sse, structured } from "./http";
export class OpenAIAdapter extends HttpProviderAdapter { readonly key = "openai"; readonly displayName = "OpenAI"; readonly defaultBaseUrl = "https://api.openai.com/v1"; readonly capabilities = new Set(["text_generation", "streaming", "structured_output", "tool_calling", "vision"] as const); async listModels(config: ResolvedProviderConfig, signal?: AbortSignal) { const body = await this.json(await this.fetcher(`${this.base(config)}/models`, { headers: { Authorization: `Bearer ${config.secret}` }, signal })); return Array.isArray(body.data) ? body.data.map((m) => this.model(String((m as { id: string }).id), "provider")) : []; } async generate(request: AIRequest, config: ResolvedProviderConfig, signal: AbortSignal): Promise<AIResponse> { const body = await this.json(await this.fetcher(`${this.base(config)}/responses`, { method: "POST", signal, headers: { "Content-Type": "application/json", Authorization: `Bearer ${config.secret}`, ...(config.organization ? { "OpenAI-Organization": config.organization } : {}) }, body: JSON.stringify({ model: request.modelId, input: promptText(request), text: { format: structured(request) ?? { type: "text" } } }) })); const text = String((body.output_text ?? "")); return { text, structured: parseStructured(request, text), usage: this.normalizeUsage(body.usage), providerRequestId: String(body.id ?? "") }; } async *stream(request: AIRequest, config: ResolvedProviderConfig, signal: AbortSignal): AsyncIterable<AIStreamEvent> { const response = await this.fetcher(`${this.base(config)}/responses`, { method: "POST", signal, headers: { "Content-Type": "application/json", Authorization: `Bearer ${config.secret}` }, body: JSON.stringify({ model: request.modelId, input: promptText(request), stream: true, text: { format: structured(request) ?? { type: "text" } } }) }); if (!response.ok) throw { status: response.status }; yield* sse(response, signal); } }
